{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Frontend (Vite)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "bun: dev",
      "sourceMapPathOverride": {
        "${webRoot}/*": "${workspaceFolder}/src/*"
      }
    }
  ]
}
